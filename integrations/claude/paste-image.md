Read the latest screenshot synced from the user's laptop via clipremote.

1. Read the image file at `~/.cache/clipremote/latest.png` (use the Read / multimodal image tool).
2. Describe or act on it according to the user's request.
3. If the file is missing, tell them to take a new screenshot into their clipremote-watched folder (usually Desktop or Screenshots) and wait a second for auto-sync, then retry.
4. Do not ask for `scp` or laptop absolute paths.
